This is the beggining of my updated game engine.

2025

25 jan
-Created project SandCastle.

July

18

-Decided to copy dependencies into the project (no submodules).
-build folders
-added distrib config
-decided to keep include the same way it was.

24

-built all the dependencies
-progress on migration
-progress on build config (done ?)

26

-progress on migration to sdl3 entt latest

27

-rebuilt imgui to include sdl3
-addressed some warnings.
-created Test project
-completed build!
-fixed linking problems (project references...)
-fixed (?)  ENTT_ASSERT (still need to test in case of real failure)
-created a window with no bugs!
-decided to keep singleton (bc the only point I see is to avoid user doing shit before he calls Engine::Init)


28

-.gitignore
-first commit/push!
-draw sprite test success.
-removed useless defines
-imgui no longer available in distrib
-camera default constructor updated to match comon use case
-systems have its own default camera
-systems init
-have a camera by default in the init
-entity create for easily created animated sprite or sprite
-tested entity create

29

-removed pch.h in Files.h
-renamed "Add..Input" to "Bind..Input"
-input test
-begginer friendly tests
-renamed header
-rotations and vector directions are now coherent
-removed time operator % / * cause they make no sense for duration and * was making a recursive call
-created Geometry namespace and moved the approriate funciton there

30

-Entt assertion break and logging correctly
-Test distrib build without console (this involved changing linker advanced settings to configure the entry point )
-Improved serialized error message
-moved input system
-changed order of glBindFrameBuffer and glViewport bc apparently there was a mistake
-find out pixel/point size is the same regardless of windows scaling factor (at least on my machine)
-fire only one resize signal when pixel size change (not when only window size change, but they always trigger both)
-couldn't figure what the point size could be used for
-therefore using only the pixel size for everything.
-window event test
-Gamepad, mouse, key namespace

August 

01

-Serialized: HadGetError, HadParseError, IsNull, (currently HadGetError is merged with HadParseError)
-Serialized: GetObjArray
-Serialized: "ResetGetError()"
-Serialized: bool TryGet

02

-Progress on improving the renderer
-checked if we can offload matrice computation to gpu
(I think we cannot because of transform parenting)

03

-deleted copy constructor for Asset
-raw pointer in asset
-simplifed MakeAsset
-Asset::Get now gives a raw pointer
-replaced many shared ptr by raw pointer where better suited
-improved const correctness
-removed useless Draw method to simplify Renderer2D

04

-decided we don't touch line and wires but we ackowledge that
they should not be used because of sorting issue
-making QuadRenderData (that's the objects of the double buffer)

05

-progress on instanced rendering

06

-instanced rendering
-refacto of the renderer.
non exhaustive list:
(-QuadRenderData
-texture as ID
-removed render options for individual quad/sprite)
-gave up on instancing, it's actually shit.
-benchmark against sandbox: same fps, shifted load on GPU which is great (at least on my config)
120fps= 0.008s total, gpu time (buffer data + draw call)= 0.0016s vs 0.0012s on sandbox
-materials!

07

-I managed to draw from the thread!
-Worker thread improvement: Wait method
-fixed blinking at low frame rame: the same buffer getting filled twice if cpu cycle is faster than

9

-progress on new delegate

10

-new delegates more efficient (no type erasure, more flexible)
-moved the type erasure to signal
-signal adjusted to new delegates
-signal static method fixed

12

-fixed signal for real now
-improved worker, easier to push tasks
-basic profiling
-moved vertex and uv computation to CPU: gained 100fps :ooo
-fixed crash on exit program
-renamed OnRender (no longer accurate) to OnLateUpdate
-imgui within the render thread (I guess)

14

-delegate/task with no argument work properly
-figured out viewport was'nt actualizing
-add layer thread safe
-on window size signal fixed
-on window size thread safe

18

-no more SolutionDir used to reference dependencies or build binaries
-ignoring assets files with no extension
-various minor fixes
-removed useless files
-Test do not depend on SDL3
-shaders now include aspect ratio and window height as part of a uniform block called "scene"

19

-learned that view should never be created concurrently

20

-investigated fix for font issue

21

-finally fixed the character not showing issue. The trouble came from thread rendering:
Allocating gpu memory (texture atlases) without syncing with the render thread.
-Texture now sync between update and bind
-do the fancy macros <3
-font test done.

22

-view should always iterate on all the components needed.
-using the SandCastle Each wrapper result in a significant loss of perf.
-therefore I got rid of all the SandCastle wrapper (ForeachEntities and such);
-Entity::View
-Entity::DestroyAll
-making 1 component view THEN getting the comp is much less efficient
-group for two components is faster.
-creating view/group on the go doesnt make significant diff.
-using group for sprite render now

23

-groups: only for internal usage
-have a DestroyAll<> method
-null spriterender check in sprite render ssystem
-renamed system methods, removed delta
-layer sorting
-per layer z sorting (enabled/disabled)

24:

-renamed camera wordToScreenRatio -> zoom
-renamed default shader: default
-found the best pixel perfect way: tex ppu = 1, cam zoom = 0.00277777f
-so no fancy pixel prefect shader are needed
-just keep resolution to 720 or 1080 and such.
-sprite origin with half float to keep QuadRenderData tight
-camera main as static field
-mouse::getworldpos
-inputs fancy getter
-pix perfect game example

27

-Camera ensure ratio
-Camera pixel perfect
-Camera black bar (crop rendering area)

28

-texture create from subregion
-sprite asset Y swapped for more intuitive reading
-sprite texture rect kept and accessible
-basic UI frame drawing.

30

-fixed font drawing
-cleaned texture update region
-fixed material properties
-fixed zsort
-new batches

31

-window layer showing behind other layer is not related to some layers not showing at all
-fixed layer not showing
-add color unsigned char to QuadData, replace uvOrColor
-replace m_maxOffscreen layer by define
-fixed font using layers
-material knows if its for layer or not to avoid creating useless batches
-fixed sampler2D[] material error message
-rename Writer Writer
-Writer is held by Ui
-Ui is a singleton

September

01

-fancy name to font
-write can use custom material and layer
-fonts assets should be handled by Assets
-separate sprite template creations from frame creation
-debug null texture during uitest
-debug incorrect position of frame sprites

02

-test frame creation from template
-margin and edge extrude for frame borders seem unecessary atm with UiTest at least
-remove push/pop mat/layer/font, make it Set
-StartCanvas
-add a text inside
-EndCanvas

11

-text over canvas (z)
-text anchor point
-learned that depth testing cannot be reliable in one single draw call.
-error if no canvas during elem creation.
But I am very suspicious of that...
-YEAH! the culprit was the blending func!
-Now there's a classic alpha blending for quads
-And a premultiplied alpha blending for layers
-everything draw nicely as expected \o/
-Added access to default quad and layer materials
-Added alpha discard as a uniform (default no discard.);
-The alpha discard can be used to replace Z sort when there's no transparent fragments!

12

-UiTxt know it's own size
-Text wrapping respect words
-canvas Y stretch
-fixed sentence y size.
-centered text
-right aligned text
-remove moving sentence to next line when wrapping a single word
-fix text alignement (\n works, wrapping dont)
-simplify space adv
-don't create entity for spaces
-fix text alignement center and right (remove the space adv)
-add render option into material
-remove render option for layer then (because it would be doublon);
-(set ui material render option depth test to false (to fix overlaping depth test fight)
-text margin
-cursor of current element in the current canvas
-element SetPosition
-text set position teke margin into account
-text size take margin into account


13

-think about layout.
-layout (leftright, topdown...)
-layout wrapping: when reaching the max, where do we go ?
-layout align: when reaching the max
-basic layout working. (top down, wrap normal)
-instead of advancing cursor after each elem creation, create every elems, then arrange the layout.
-basic layout with lambdas working (clean implementation)
-elem anchor 
-elem anchor automatic depending on layout dir & wrap
-good progress on UI overall

14

-removed inverse wrapping and just have TopDown or LeftRight layout dir.
-alignement works.
-frame Z based on elem Z
-add Image
-to test better the layouts
-parent canvas children root canvas
-move canvas with anchor works
-nesting canvas
-fixed z fighting in nested canvas

15


-add Canvas spacing (between element, no matter the direction/wrapping)
-Add canvas border
-renamed padding margin for consistency between elemnt and canvas
-limit nested canvas stretch to parent if fixed
-root margin, elem margin (including nested canvas)

16

-fixed
-the
-fucking
-layout
-one
-more
-time.
-fixed stretching for nested canvas
-done more ui test
-canvas size match frame size.
-update position on make layout

17

-UI header and folder
-window even px dim.
-ui default material
-ui ppu
-positions conversion
-added Bind WASD for dir input 
-test positions conversions methods

18

-fixed pointer definition of frames
-moved frames data to entity 
-elem roots are default constructed
-image root != image entity to ensure top left anchoring
-txt override the root to avoid unecessary transform computation for each letter
-count listener
-Any elem is able to be hoverable/clickable (callbacks)
-ui elem hitbox and update with position
-make elem hoverable/clickable by adding a listener
-interface over signal
-button
-text maxWidth => width
-update text

19

-fixed camera not recomputing view matrix on aspect ratio change
-fixed ui hitbox computation
-fixed ScreenToUi computation
-round pos of ui elements (fixed the button deformation)
-round pos of frame sprite
-but the real deal is to not use floating point number for spacing, margin, positioning etc..
-For pixel art it is NECESSARY to have the font matching the current dpi (so one font dpi for each step)
-found all canvas deformations issues: font dpi included
-transform no longer compute matrix (useless atm)
-Set filtering for writer
-Floored to even number every viewports
-so removed window size enforcing to even number
-Writer custom color
-Update text keep font
-text value works

20

-Canvas elem vector->list
-moved frame into UiFrame
-UiElem state (hover, pressed...)

21


-Canvas have authority to delete children
-Ui has authority to delete root canvases
-Clean deletion of pointed objects
-basic test for memory leaks
-elem delete: must be removed from m_hoverable, hovered, m_values..
-fixed button frames not right

22

-destroy clean refacto

23

-seems like ark pixel isn't the good font maybe, missing important ja character
-fixed the sprite* problem: the fact is that I was pointing to a component and the component can change place..
std::vector avoid this problem.
-simplified transform implementation trying to optimize

24

-dont parent ui element, but move them with SetPosition:
-UiElem have a children<entity> list for movement
-update canvas depending on parent count to update children size before parent
-removed UiElem root constructor
-img remove entt and override SetPosition/GetPosition
-uDiscardAlpha default value is 0.05f
-canvas padding (global state)
-no writer ppu = 3 in ui.cpp
-test hover create a canvas
-elem->z must reflect the current canvas layer
-checkbox

25

-added line adjustement
-canvas context
-removed old UiTest (only uipostest)
-text width is maxPen
-only one big ui context
-rounded alignement of text to fix a deformed pixel art text issue
-root anchor: context
-many ui tests

26

-shove transform from matrix and other useless stuff and benchmark
-Ui bool OnEvents, remove the Inputs from UI
-check if UI blocking event works
-left click UI
-get rid of inputs system: merge into inputs singleton
-destroy test
-removed fast roots
-improved checkbox with bool pointer
-create memory leak test
-Ui::Destroy(all kind of elem&*) to nullptr the pointer
-Engine init with settings or path to settings
-default import texture settings can be set in the engine settings.

27

-fixed the localized texture issue.
-localization built into the asset working
-Asset must check if default lang is part of available lang

28

-fallback lang is default lang
-assert that fallback lang has every lang
-have a fallback lang for non-localized: especially for texture like game title
-assert when fallback lang does not have a localized asset
-opti assets: each asset type has its own container.
-using the .sln shipped with freetype to fix the static issue, manually copying the freetype.lib in the vendor folder...

29

-added textual asset
-UiTextLoc with UiTxt that change loca automatically

30

-writer has possibly lang for fonts
-Ui make font integrate langs
-btn localized
-fonts localiezd
-mutexed imgui events and render with the hope it removes crashes
-versioning
-custom asset folder
-roaming system

October

1

-rate

2

-sounds

5

-sound as ui context button click
-fixed mloading sound and memory allocation 

9

-the problem with font scaling is that font become blurry when resolution is 360p
-Silver.tff and LanaPixel.tff are good candidates for being font
-font now has a line height

12

-renamed OnFixedUpdate
-Delegate discrimate by method name call arg and call free fun/method
-post start
-Add elem to already existing canvas
-basic elem order

13

-arbitrary data

14

-custom canvas Z order (with context)
-block interaction with canvas
-clean update layout and m_hovered when destroy

16

-canvas about to destroy will no longer be added in must update
-m_destroy should no longer contain double entry
		
		
22

-Systems::Push returns the created system
-math vec to angle rotation fixed

>add text to canvas should re create text to set limit width
>fix margin (see Ui::NewElem)
>animated sprite in UI
>check what is the line return behaviour with ja/cn
>when lang is any lang that has different breaking rule (cn or ja)
make it impossible to line return on the character that should not be at the beggining of a sentence
>context for checkbox sound


>rename OnImGui oand other On without the On
tasks:

>vsync not working
>Implement steamworks from idle colony
>game system pattern: Plmp, data in registry, signal are pointers.
no need plmp if data is in registry and communication is only through signal?
>rename Filter to MaskFilter and use template
>Implement a default GameSystem class with state machine
>math inverselerp
>japanese (chinses?) breaking rules
>visual loading during engine Init (especially for assets)
>reintegrate the audio spike protection 

secondary tasks:

>bake sentence into one texture. have two type of text: dynamic (single entity character), static (baked)
>text in UI could break out of the canvas frame if its positioned right of another element
possible solution: realign text after Write bazed on new size at the end of MakeLayout
>text input (SDL_StartTextInput...)
>audio spacialization
>Transform animation system
>fnv1a for strings like sounds
>check for useless headers
>optimize nested transform?
>edge cases with sprite deformation (notably with ui text), example: very large window but still render is 360p
>UiElem have a specific layout order (by default order of creation)
>UiElem can change parent
>overlapp canvases without z fighting
>scrolling
>UiElem can be floating: have the same position of the last elem, or custom...
>thread safety in profiler
>opti: sprite have texture id? + spriteRender have texture id + all sprite relevant data to reduce cache miss
(and then for localization sprite render with localized sprite must listen to OnChangeLoca)

bugs:

>F12 create a crash
>asset log error not always log in the .txt logs?

render:


>quad render camera culling
>reintegrate wire and lines (or think about it)
>Particle system?

physics:

>physics must listen to add component somewhat (re integrate listen component methods ?)
>migrate box2D 3

thoughts:

>Worker.Wait called just after queuing a task can just.. not wait really!
>unparenting a child while iterating the parent children lead to a crash because it invalidate iterator (see AssetTests.cpp in Sandbox)
>Serialized, Get error must propagate to all parents?
>Transform rotation should loop at 360/-360? Because we could potentially break the float limit.
But I'm not happy with the additional processing required, so I'm letting the user do that if they want.
